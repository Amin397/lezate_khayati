import 'package:flutter/material.dart';

import '../../../Plugins/get/get.dart';



class PublicTrainingScreen extends StatelessWidget {
  const PublicTrainingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: Get.height,
      width: Get.width,
      color: Colors.grey[200],
    );
  }
}
